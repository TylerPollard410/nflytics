% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/latent_strength_api.R
\name{gq_latent_strength}
\alias{gq_latent_strength}
\title{Run generated quantities for the latent strength model}
\usage{
gq_latent_strength(
  object,
  schedule_idx,
  targets = NULL,
  horizon = NULL,
  gq_model = NULL,
  seed = NULL,
  parallel_chains = NULL,
  sig_figs = NULL,
  ...
)
}
\arguments{
\item{object}{An \code{nflytics_fit} created by \code{\link[=fit_latent_strength]{fit_latent_strength()}}. Only
fits obtained with \code{engine = "cmdstanr"} are supported.}

\item{schedule_idx}{Data frame describing the game schedule with the index
columns expected by \code{\link[=prepare_schedule_indices]{prepare_schedule_indices()}}, including (at minimum)
\code{season_idx}, \code{week_idx}, \code{fw_season_idx}, \code{lw_season_idx}, \code{home_idx},
\code{away_idx}, and \code{hfa}. Additional descriptive columns such as
\code{season}, \code{week}, \code{home_team}, and \code{away_team} are preserved and attached
to the result for downstream summaries.}

\item{targets}{Vector of global \code{week_idx} values identifying the games to
forecast, or a data frame with columns \code{season} and \code{week} that can be
matched against \code{schedule_idx}. When \code{NULL}, supply \code{horizon} to request
the next \code{horizon} weeks automatically.}

\item{horizon}{Optional integer specifying how many weeks beyond the fitted
window to score. Ignored when \code{targets} is supplied.}

\item{gq_model}{Optional pre-compiled CmdStan model returned by
\code{\link[cmdstanr:cmdstan_model]{cmdstanr::cmdstan_model()}}. When omitted the helper compiles (or reuses)
\code{latent_strength_ssm_gq.stan}.}

\item{seed}{Optional RNG seed forwarded to
\code{\link[cmdstanr:model-method-generate-quantities]{cmdstanr::generate_quantities()}}. Defaults to the seed stored on the fit
object when available.}

\item{parallel_chains}{Optional number of parallel chains used by
\code{generate_quantities()}. By default the function reuses the number of fit
chains (bounded by the detected core count).}

\item{sig_figs}{Optional significant-figure setting passed to
\code{generate_quantities()}.}

\item{...}{Additional arguments forwarded to
\code{\link[cmdstanr:model-method-generate-quantities]{cmdstanr::generate_quantities()}}.}
}
\value{
An object of class \code{nflytics_gq} containing the generated quantities
draw set, schedule metadata, and the Stan data used for prediction.
}
\description{
Executes the standalone generated-quantities program
\code{latent_strength_ssm_gq.stan} using an existing \code{nflytics_fit} so that
out-of-sample forecasts can be produced without refitting the core model.
}
\seealso{
\code{\link[=fit_latent_strength]{fit_latent_strength()}} for model fitting.
}
