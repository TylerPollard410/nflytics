% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan_helpers.R
\name{run_sequential_backtest}
\alias{run_sequential_backtest}
\title{Run Sequential Backtest}
\usage{
run_sequential_backtest(
  fit_mod,
  gq_mod,
  schedule_df,
  teams,
  start_week,
  end_week = NULL,
  output_dir = "backtest_results",
  fit_params = list(chains = 4, parallel_chains = 4, init = 0, sig_figs = 10, iter_warmup
    = 1000, iter_sampling = 1000, thin = 1, adapt_delta = 0.9, max_treedepth = 10, seed =
    52),
  verbose = TRUE,
  use_simple_extraction = TRUE,
  skip_refit_if_incomplete = TRUE,
  retain_results = FALSE
)
}
\arguments{
\item{skip_refit_if_incomplete}{Logical; when TRUE the routine skips refitting if the
next week in the backtest window lacks observed results (forecast-only weeks)}

\item{retain_results}{Keep full per-week results in memory (default FALSE stores
only file paths + metadata to reduce RAM usage)}
}
\description{
Runs sequential backtesting starting from an initial fit window
}
