% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/latent_strength_api.R
\name{fit_latent_strength}
\alias{fit_latent_strength}
\title{Fit the latent strength model using Stan}
\usage{
fit_latent_strength(
  data,
  engine = c("rstan", "cmdstanr"),
  iter_warmup = 1000,
  iter_sampling = 1000,
  chains = 4,
  parallel_chains = min(chains, max(1, parallel::detectCores(logical = FALSE) - 1)),
  seed = NULL,
  refresh = 50,
  adapt_delta = 0.9,
  max_treedepth = 12,
  init = 0,
  ...
)
}
\arguments{
\item{data}{Either a data frame that can be passed to
\code{\link[=prepare_latent_strength_data]{prepare_latent_strength_data()}} or a pre-built Stan data list.}

\item{engine}{Sampling backend to use. Choose between \code{"rstan"} and
\code{"cmdstanr"}.}

\item{iter_warmup}{Number of warmup iterations.}

\item{iter_sampling}{Number of post-warmup iterations.}

\item{chains}{Number of Markov chains.}

\item{parallel_chains}{Number of chains to run in parallel (CmdStanR only).}

\item{seed}{Optional seed passed to the sampler.}

\item{refresh}{Print frequency for sampler progress.}

\item{adapt_delta, }{max_treedepth HMC tuning parameters.}

\item{init}{Initialisation strategy passed to Stan.}

\item{...}{Additional arguments forwarded to the underlying sampler.}
}
\value{
An object of class \code{nflytics_fit} containing the fitted draws and
data used for sampling.
}
\description{
Fit the latent strength model using Stan
}
